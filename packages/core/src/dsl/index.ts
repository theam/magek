/**
 * Declarative DSL for Magek
 *
 * This module provides functional APIs for defining Magek concepts (commands, events,
 * entities, etc.) without using decorators. The DSL creates the same runtime metadata
 * as decorators, allowing both styles to coexist and interoperate.
 */

export * from './command'
