name: End-to-End Tests

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - 'website/**'
      - '.github/**'
      - '.vscode/**'
      - 'packages/**/.vscode/**'

  pull_request:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - 'website/**'
      - '.github/**'
      - '.vscode/**'
      - 'packages/**/.vscode/**'

jobs:
  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build E2E Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./e2e/Dockerfile
          tags: booster-e2e:latest
          load: true
          # Disable cache for pull requests to ensure fresh builds with latest changes
          # Keep cache enabled for main branch to maintain performance
          cache-from: ${{ github.event_name == 'pull_request' && '' || 'type=gha' }}
          cache-to: ${{ github.event_name == 'pull_request' && '' || 'type=gha,mode=max' }}

      - name: Run E2E tests in Docker
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            booster-e2e:latest

      - name: Clean up Docker images
        if: always()
        run: |
          docker rmi booster-e2e:latest || true
          docker system prune -f || true