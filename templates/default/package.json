{
  "name": "{{PROJECT_NAME}}",
  "description": "{{description}}",
  "version": "{{version}}",
  "author": "{{author}}",
  "dependencies": {
    "tslib": "2.8.1",
    "@magek/core": "{{magekVersion}}",
    "@magek/common": "{{magekVersion}}",
    "@magek/cli": "{{magekVersion}}",
    "@magek/server": "{{magekVersion}}",
    "@magek/adapter-event-store-nedb": "{{magekVersion}}",
    "@magek/adapter-read-model-store-nedb": "{{magekVersion}}",
    "@magek/adapter-session-store-nedb": "{{magekVersion}}"
  },
  "devDependencies": {
    "rimraf": "6.1.2",
    "@magek/eslint-config": "{{magekVersion}}",
    "mocha": "11.7.5",
    "@types/mocha": "10.0.1",
    "@types/jsonwebtoken": "9.0.1",
    "nyc": "15.1.0",
    "typescript": "5.1.6",
    "tsx": "4.19.2",
    "@types/node": "20.17.17",
    "ts-patch": "3.1.2",
    "graphql": "16.8.1"
  },
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  },
  "homepage": "{{homepage}}",
  "license": "{{license}}",
    "main": "dist/index.js",
    "repository": "{{repository}}",
    "scripts": {
      "lint:check": "eslint \"**/*.ts\"",
      "lint:fix": "eslint --quiet --fix \"**/*.ts\"",
      "build": "tsc -b tsconfig.json",
      "dev": "tsx watch src/server.ts",
      "start": "node dist/server.js",
      "clean": "rimraf ./dist tsconfig.tsbuildinfo"
    },
  "types": "lib/index.d.ts"
}
